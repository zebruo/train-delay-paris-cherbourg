# Modèle de config.sh : copier ce fichier en config.sh puis renseigner tes
# propres valeurs (non versionné, voir .gitignore).
NAS_HOST="utilisateur@ip_du_nas"
SSH_KEY_NAS="$HOME/.ssh/id_ed25519_nas"
